{
  "scripts": {
    "vercel-build": "cd agranidigital && npm install && npm run build && mv dist ../dist",
    "dev": "cd agranidigital && npm run dev",
    "build": "cd agranidigital && npm run build",
    "preview": "cd agranidigital && npm run preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "react-router-dom": "^7.14.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "gh-pages": "^6.3.0",
    "postcss": "^8.5.9",
    "tailwindcss": "^4.2.2"
  }
}
